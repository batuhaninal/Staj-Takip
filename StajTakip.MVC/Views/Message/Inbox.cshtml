@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<Message>

<div class="text-center m-3">
    <h2>Gelen Kutusu</h2>
</div>

<table id="inboxList">
    <thead>
        <tr>
            <th>Gönderilme Tarihi</th>
            <th>Konu</th>
            <th>Mesaj İçeriği</th>
            <th>Gönderen Mail</th>
            <th>Çözüldü mü?</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count > 0)
        {
            @foreach (var message in Model)
            {
                <tr>
                    <td>@message.MessageDate</td>
                    <td>@message.Subject</td>
                    <td>
                        <div class="accordion" id="accordionExample">
                            <div class="accordion-item">
                                <h2 class="accordion-header">
                                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse_@message.Id" aria-expanded="true" aria-controls="collapse_@message.Id">
                                        Mesaj Detayları İçin Tıklayınız
                                    </button>
                                </h2>
                                <div id="collapse_@message.Id" class="accordion-collapse collapse" data-bs-parent="#accordionExample">
                                    <div class="accordion-body">
                                        @Html.Raw(message.MessageDetail)
                                    </div>
                                </div>
                            </div>
                        </div>
                    </td>
                    <td>@message.SenderUser.Email</td>
                    <td>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" asp-for="@message.IsSolved" id="flexCheckDisabled" disabled>
                        </div>

                    </td>
                    <td>
                        @if (message.IsStudentRead)
                        {
                            <div class="ml-3">
                                <label class="form-check-label" for="flexCheckDisabled">
                                    <a asp-area="" asp-controller="Message" asp-action="CheckRead" asp-route-messageId="@message.Id" asp-route-pageId="1"><span title="Okunmadı olarak işaretle!" class="badge bg-primary text-dark">Okunmadı olarak işaretle!</span></a>
                                </label>
                            </div>
                        }
                        else
                        {
                            <div class="ml-3">
                                <label class="form-check-label" for="flexCheckDisabled">
                                    <a asp-area="" asp-controller="Message" asp-action="CheckRead" asp-route-messageId="@message.Id" asp-route-pageId="1"><span title="Okundu olarak işaretle!" class="badge bg-info text-dark">Okundu olarak işaretle!</span></a>
                                </label>
                            </div>
                        }
                        @if (message.IsSolved)
                        {
                            <div class="ml-3">
                                <label class="form-check-label" for="flexCheckDisabled">
                                    <a asp-area="" asp-controller="Message" asp-action="Delete" asp-route-messageId="@message.Id"><span class="badge bg-danger">Sil</span></a>
                                </label>
                            </div>
                        }
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td></td>
                <td></td>
                <td></td>
                <td><label class="text-danger">Henüz mesaj gönderilmemiş!</label></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
        }
    </tbody>
</table>

@section Scripts{
    <script>
        $(document).ready(function () {
            $('#inboxList').DataTable();
        });
    </script>
}

