@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<StudentUser>

<div class="p-3">
    <table id="allStudents" style="width:100%;">
        <thead>
            <tr>
                <th>Ad Soyad</th>
                <th>Islemler</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Count > 0)
            {
                @foreach (var student in Model)
                {
                    <tr>
                        <td>@student.FirstName @student.LastName</td>
                        <td>
                            <a class="btn btn-square btn-success" asp-area="Admin" asp-controller="Student" asp-action="AddStudentRelation" asp-route-studentId="@student.Id" title="Öğrencilerime Ekle">
                                <span class="icon text-white-50">
                                    <i class="fa fa-user-graduate"></i>
                                </span>
                            </a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    Henuz ogrenci bulunmamaktadir!
                </tr>
            }
        </tbody>
    </table>
</div>
@section Scripts{
    <script>
        $(document).ready(function () {
            $('#allStudents').DataTable();
        });
    </script>
}